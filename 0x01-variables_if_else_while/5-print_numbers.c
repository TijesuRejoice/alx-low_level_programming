#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main - entry point
 *
 * Return: Always 0
 */
int main(void)
{
	int num;

	for (num = o; num < 10; num++)
	{
		printf("%d", num);
	}
	putchar('\n');
	return (0);
}
