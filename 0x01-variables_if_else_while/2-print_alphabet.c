#include <stdio.h>
/**main- entry point of code, all codes are here
 * using putchar only twice
 * return successful
 */
int main()
{
	int ch;
	for (ch='a'; ch<='z'; ch++)
		putchar(ch);
		putchar("\n");
	return (1);
}

